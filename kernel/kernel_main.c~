#include "vga/vga.h"

void kernel_main()
{
    vga_instance vga = init_vga(80, 25);
    set_vga_colors(&vga, VGA_LIGHT_GREY, VGA_BLACK);

    write_char_vga('H', vga.cursor_x++, vga.cursor_y, &vga);
    vga_update_cursor(&vga); 
}
